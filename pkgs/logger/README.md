# logger

Reusable logging helpers for applications in this monorepo.

## Available helpers

- LogRecord: structured log model with standard fields.
- append_log_record(file_path, record): append one JSON log line to a file.
